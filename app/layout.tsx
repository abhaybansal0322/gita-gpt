import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Krishna Wisdom - Divine Guidance from Bhagavad Gita',
  description: 'Seek spiritual guidance from Lord Krishna and the eternal teachings of the Bhagavad Gita. Get divine wisdom for your life\'s questions.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}